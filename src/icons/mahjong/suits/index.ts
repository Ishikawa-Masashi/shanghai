export enum Suits {
  Dots,
  Bamboo,
  Characters,
}

export function getSuitedTileFrom(suits: Suits, x: number) {
  //   if (icons[name]) {
  //     return icons[name];
  //   }
  //   return (width: number, height: number) => (
  //     <MJ1tiao width={width} height={height} />
  //   );
}
